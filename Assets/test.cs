using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class test : MonoBehaviour
{ /*
    public Transform other;
    public GameObject world;
    public GameObject player;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if (other)
        {
            Vector3 forward = transform.TransformDirection(Vector3.up);
            Vector3 toOther = other.position - transform.position;

            if (Vector3.Dot(forward, toOther) < 0)
            {
                world.transform.RotateAround(player.transform.position, Vector3.up, 10 * Time.deltaTime); // Ta funcja powoduje że CameraRig się obraca
                Vector3 globalnyYlokalnie;
                globalnyYlokalnie = transform.InverseTransformDirection(Vector3.up);
            }
        }
    }
    */
    
    
}
