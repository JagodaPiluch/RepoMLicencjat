using System.Collections;
using System.Collections.Generic;
using System.Collections.Specialized;
using System.Threading;
using UnityEngine;

public class Rotation : MonoBehaviour
{ /*
    public GameObject player;
    public GameObject world;
    public float speed;



    void Start()
    {
        
    }
  
    double Angle(Vector3 position, Vector3 target)
    {
      // Vector3 inverse = position, target, diference;
      // inverse = transform.InverseTransformDirection(Vector.up);

        Vector3 diference = Vector3.zero;

        if (target.x > position.x)
            diference = target - position;
        else
            diference = position - target;
       
      //  double angleBetween;
        var angleBetween = Vector3.Angle(Vector3.right, diference);
        return angleBetween;
      //  Vector3 a = angleBetween; 
     }
        
    void Update()
    {
       
        world.transform.RotateAround(player.transform.position, Vector3.up, 10 * Time.deltaTime); // Ta funcja powoduje że CameraRig się obraca // zamiast time.deltatimr to wpisać diference
        Vector3 globalnyYlokalnie;
        globalnyYlokalnie = transform.InverseTransformDirection(Vector3.up);
       // player.transform.rotation *= Quaternion.AngleAxis(speed, Vector3.up);
       // transform.rotation = Qaternion.Inverse(player.rotation);
       
    }
    */
}
